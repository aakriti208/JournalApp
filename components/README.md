# Components

This directory contains reusable React Native components for the journal app.

## Structure

- UI components (buttons, inputs, cards)
- Journal-specific components (entry cards, calendar views)
- Layout components (headers, navigation)
